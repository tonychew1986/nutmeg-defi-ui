/*
 * TreasuryPoolEntry Messages
 *
 * This contains all the text for the TreasuryPoolEntry component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TreasuryPoolEntry';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TreasuryPoolEntry component!',
  },
});
