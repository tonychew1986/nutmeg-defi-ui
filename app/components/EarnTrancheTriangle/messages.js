/*
 * EarnTrancheTriangle Messages
 *
 * This contains all the text for the EarnTrancheTriangle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EarnTrancheTriangle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EarnTrancheTriangle component!',
  },
});
