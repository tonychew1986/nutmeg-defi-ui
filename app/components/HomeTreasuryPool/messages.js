/*
 * HomeTreasuryPool Messages
 *
 * This contains all the text for the HomeTreasuryPool component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HomeTreasuryPool';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeTreasuryPool component!',
  },
});
