/*
 * EarnTranchePopup Messages
 *
 * This contains all the text for the EarnTranchePopup component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EarnTranchePopup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EarnTranchePopup component!',
  },
});
