/*
 * AvailablePools Messages
 *
 * This contains all the text for the AvailablePools component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AvailablePools';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AvailablePools component!',
  },
});
