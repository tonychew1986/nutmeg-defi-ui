/*
 * ContractAddress Messages
 *
 * This contains all the text for the ContractAddress component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ContractAddress';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContractAddress component!',
  },
});
