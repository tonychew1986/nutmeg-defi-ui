/*
 * TransactionWait Messages
 *
 * This contains all the text for the TransactionWait component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TransactionWait';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransactionWait component!',
  },
});
