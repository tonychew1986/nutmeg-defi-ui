/*
 * LoginAccessPrompt Messages
 *
 * This contains all the text for the LoginAccessPrompt component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LoginAccessPrompt';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoginAccessPrompt component!',
  },
});
