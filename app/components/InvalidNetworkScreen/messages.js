/*
 * InvalidNetworkScreen Messages
 *
 * This contains all the text for the InvalidNetworkScreen component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InvalidNetworkScreen';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InvalidNetworkScreen component!',
  },
});
