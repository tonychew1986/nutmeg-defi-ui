/*
 * AssetInput Messages
 *
 * This contains all the text for the AssetInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AssetInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssetInput component!',
  },
});
