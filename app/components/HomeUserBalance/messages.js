/*
 * HomeUserBalance Messages
 *
 * This contains all the text for the HomeUserBalance component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HomeUserBalance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeUserBalance component!',
  },
});
