/*
 * CommunityPage Messages
 *
 * This contains all the text for the CommunityPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommunityPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommunityPage container!',
  },
});
