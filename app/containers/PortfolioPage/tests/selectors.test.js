// import { selectPortfolioPageDomain } from '../selectors';

describe('selectPortfolioPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
